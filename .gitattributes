# Отключить автоматическую нормализацию переводов строк (core.autocrlf):
# дистрибутив содержит файлы с фиксированными кодировками и переводами строк
# (.ps1 - UTF-8 с BOM, .bat с русским текстом - CP1251, .vbs - ASCII,
# .ico/.png - двоичные), поэтому в репозитории они должны храниться байт-в-байт.
* -text
